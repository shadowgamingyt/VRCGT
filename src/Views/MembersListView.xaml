<UserControl x:Class="VRCGroupTools.Views.MembersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Main Content -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="👥 Members" FontSize="24" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="View and manage all members in the selected group. Click a name to edit." Foreground="#888" FontSize="12"/>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Refresh"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource MaterialDesignRaisedDarkButton}" 
                            Background="#7C4DFF"
                            Height="36" Width="100"/>
                    <Button Content="Load Cached"
                        Command="{Binding LoadFromCacheCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Height="36" Width="120"
                        Margin="10,0,0,0"/>
                    <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="🔍 Filter by name or userId"
                             Width="220"
                         Margin="10,0,0,0"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Foreground="White" CaretBrush="White"/>
                    <ComboBox ItemsSource="{Binding RoleFilters}"
                              SelectedItem="{Binding SelectedRole}"
                              Width="160"
                              Margin="10,0,0,0"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Foreground="White"/>
                    <TextBlock Text="{Binding Status}" Foreground="#7C4DFF" Margin="15,8,0,0" FontSize="13"/>
                </StackPanel>
            </StackPanel>

            <!-- Members List -->
            <Border Grid.Row="1" Background="#2D2D30" CornerRadius="8" Padding="0">
                <DataGrid ItemsSource="{Binding FilteredMembers}"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          Background="Transparent"
                          RowBackground="#2D2D30"
                          AlternatingRowBackground="#252528"
                          Foreground="White"
                          GridLinesVisibility="None"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <!-- Clickable Display Name -->
                        <DataGridTemplateColumn Header="Display Name" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DisplayName}" 
                                               Foreground="#7C9CFF" 
                                               Cursor="Hand"
                                               TextDecorations="Underline"
                                               VerticalAlignment="Center"
                                               Margin="8,0">
                                        <TextBlock.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" 
                                                          Command="{Binding DataContext.SelectMemberCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          CommandParameter="{Binding}"/>
                                        </TextBlock.InputBindings>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="User ID" Binding="{Binding UserId}" Width="2*"/>
                        <DataGridTextColumn Header="Joined" Binding="{Binding JoinedAt}" Width="*"/>
                        <DataGridTextColumn Header="Roles" Binding="{Binding RoleSummary}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Member Edit Panel -->
        <Border Grid.Column="1" 
                Width="350"
                Background="#252526"
                CornerRadius="8"
                Margin="20,0,0,0"
                Padding="15"
                Visibility="{Binding ShowMemberPanel, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Header with Close Button -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Edit Member" Foreground="White" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                    <Button Content="✕"
                            HorizontalAlignment="Right"
                            Command="{Binding CloseMemberPanelCommand}"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="#888"
                            Width="30" Height="30"/>
                </Grid>
                
                <!-- Panel Content -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Loading -->
                        <ProgressBar IsIndeterminate="True" 
                                     Margin="0,20,0,0"
                                     Foreground="#7C4DFF"
                                     Visibility="{Binding IsLoadingMember, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        
                        <!-- Member Info -->
                        <StackPanel Visibility="{Binding IsLoadingMember, Converter={StaticResource InverseBoolToVisConverter}}">
                            <!-- Display Name -->
                            <TextBlock Text="{Binding SelectedMember.DisplayName}"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       TextWrapping="Wrap"
                                       Margin="0,10,0,0"/>
                            
                            <!-- User ID -->
                            <TextBlock Text="{Binding SelectedMember.UserId}"
                                       Foreground="#666"
                                       FontSize="10"
                                       HorizontalAlignment="Center"
                                       Margin="0,5,0,0"/>

                            <Button Content="View Full Profile"
                                    Command="{Binding ViewFullProfileCommand}"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Height="30"
                                    Margin="0,10,0,0"
                                    Foreground="#2196F3"
                                    BorderBrush="#2196F3"/>
                            
                            <!-- Joined Date -->
                            <Border Background="#1E1E1E" CornerRadius="8" Padding="12" Margin="0,15,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="Joined: " Foreground="#888" FontSize="12"/>
                                    <TextBlock Text="{Binding SelectedMember.JoinedAt}" Foreground="#CCC" FontSize="12"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Current Roles -->
                            <Border Background="#1E1E1E" 
                                    CornerRadius="8" 
                                    Padding="12"
                                    Margin="0,15,0,0">
                                <StackPanel>
                                    <TextBlock Text="Current Roles" Foreground="#888" FontSize="11" Margin="0,0,0,8"/>
                                    <ItemsControl ItemsSource="{Binding MemberRoles}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#3D3D40" 
                                                        CornerRadius="4" 
                                                        Padding="8,4" 
                                                        Margin="0,0,0,5">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Name}" 
                                                                   Foreground="#CCC"
                                                                   VerticalAlignment="Center"/>
                                                        <Button Grid.Column="1"
                                                                Content="✕"
                                                                Command="{Binding DataContext.RemoveRoleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                Style="{StaticResource MaterialDesignFlatButton}"
                                                                Foreground="#F44336"
                                                                Width="24" Height="24"
                                                                Padding="0"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="No roles assigned" 
                                               Foreground="#666" 
                                               FontSize="11"
                                               Visibility="{Binding MemberRoles.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Assign Role -->
                            <Border Background="#1E1E1E" 
                                    CornerRadius="8" 
                                    Padding="12"
                                    Margin="0,15,0,0">
                                <StackPanel>
                                    <TextBlock Text="Assign Role" Foreground="#888" FontSize="11" Margin="0,0,0,8"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox ItemsSource="{Binding GroupRoles}"
                                                  SelectedItem="{Binding SelectedRoleToAssign}"
                                                  DisplayMemberPath="Name"
                                                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                                                  Foreground="White"/>
                                        <Button Grid.Column="1"
                                                Content="+"
                                                Command="{Binding AssignRoleCommand}"
                                                Width="40" Height="40"
                                                Margin="10,0,0,0"
                                                Style="{StaticResource MaterialDesignRaisedDarkButton}"
                                                Background="#4CAF50"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            
                            <!-- Moderation Actions -->
                            <Border Background="#1E1E1E" 
                                    CornerRadius="8" 
                                    Padding="12"
                                    Margin="0,15,0,0">
                                <StackPanel>
                                    <TextBlock Text="Moderation Actions" Foreground="#888" FontSize="11" Margin="0,0,0,8"/>
                                    
                                    <Button Content="⚠️ Issue Warning"
                                            Command="{Binding WarnMemberCommand}"
                                            Height="36"
                                            Margin="0,0,0,8"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Foreground="#FFC107"
                                            BorderBrush="#FFC107"/>
                                    
                                    <Button Content="👢 Kick from Group"
                                            Command="{Binding KickMemberCommand}"
                                            Height="36"
                                            Margin="0,0,0,8"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"
                                            Foreground="#FF9800"
                                            BorderBrush="#FF9800"/>
                                    
                                    <Button Content="🚫 Ban from Group"
                                            Command="{Binding BanMemberCommand}"
                                            Height="36"
                                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                                            Background="#F44336"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
